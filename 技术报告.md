# 《实验史记·马刀行》技术报告

**版本**：1.1（2026-09-15） · **形态**：单文件 HTML 网页游戏（双击即玩，零依赖）
**前作**：《实验史记·马刀风云》（回合制马刀战棋）、《实验史记·春秋笔》（校园探索+社交+撰史）
**本作定位**：马刀开放世界版 —— 校园箱庭做壳，马刀风云的战棋做全部战斗内核，剧情架构由「章节×时段×行动点」重构为任务驱动的自由开放世界。
**1.1 变更摘要**：废除时间线门控（章节改由主线驱动）、立传降级为可选支线、新增开局难度入口与任务指引、修复 5 处 `const` 全局判活失效的静默 bug。

---

## 1. 项目目标与总体架构

用户需求：把"马刀"做成开放世界游戏；开放世界参考第二个游戏（春秋笔）；探索到不同地方进入不同支线、发生战斗；战斗机制即马刀（第一个游戏的猜拳战棋），主流玩法就是马刀；剧情参考第二个游戏。

采用**双基座融合**策略，而非重写：

```
┌────────────────────────── 融合层（本项目新写 ~3000 行）─────────────────────────┐
│  quests.js 主线9节+支线7条+难度表   blades.js 刀谱成长                          │
│  battle-ui.js 战斗覆盖层   main.js 择难度/约战/结算/锦标赛/终章                  │
├────────────────────────── 世界层（春秋笔基座，剧情架构已重构）──────────────────┤
│  world.js 世界渲染·寻路·NPC调度·任务标记   engine.js 进度·资源·风险   dialog/ui  │
│  writing.js 撰史（已与主线解耦）   data/ people(34人) scenes(12景) events(68)     │
├────────────────────────── 战斗层（马刀风云整体移植）────────────────────────────┤
│  battle/engine.js 战斗引擎(DOM无关·9种剧情特则)  battle/data.js 33角色卡          │
│  config/audio/save   battle/scenes.js 剧本卷(战前/战胜/战败台词)                 │
└──────────────────────────────────────────────────────────────────────────────┘
```

关键解耦决策：

1. **战斗引擎不依赖 DOM**（继承马刀风云的既定设计），通过 `window.SJI_UI` 钩子（`rpsRound/playerPhase/pickBoon/onBattleEnd/fx*`）与覆盖层通信，因此可在 Node 里裸跑测试——前作 15 个引擎测试原样跑绿。
2. **世界与战斗通过两条钩子对接**：世界侧调 `SJI_UI.startBattle(cfg)` 开战；战斗结束由 `SJI_BATTLE_HOOKS.onResult(b)` 计算奖励/录技/成就/任务推进并返回结算文案，`onDone()` 恢复世界（并消费挂起的章节里程碑卡）。两侧互不 import，靠全局命名空间（`SJI_*` 与世界扁平全局）天然隔离。
3. **数据驱动**：加一节主线/一条支线只改 `js/quests.js` 的数据表；加一个可约战 NPC = 在 `people.js` 有社交数据 + 在 `battle/data.js` 有战斗卡，约战按钮与首战剧本自动生效。
4. **章节由任务推导，不由时间累积**：`Quests.chapterNow()` 把主线完成度映射为 `G.ch`，`Engine.syncChapter()` 单向推进。旧内容只认 `G.ch` 阈值（协会 ≥8、刀禁期 ≥12、番外 ≥15），因此一处映射表就把"剧情进度"接回了全部既有系统。

## 2. 融合层设计

### 2.1 刀谱系统（blades.js）——白板开局 → 修炼 → 录技的成长闭环

- **白板开局**：音克思初始无任何技能（技能按钮置灰"无技"），全靠买刀买马与刀击马踢作战；赢下第一场，`Blades.grant` 自动装备对手之技——第一技即首胜的战利品。
- **主角卡**：玩家在世界里是音克思，在马刀场上也是音克思（charId `yinkesi`）。`Blades.registerChar()` 按当前胜场与修炼等级实时合成战斗卡注册进 `SJI_DATA.CHARACTERS`：血上限 `10 + min(4, floor(wins/8)) + 修炼体魄×2`。
- **录技**：首次击败某角色，其技能定义（含出处注）拷贝入谱，可在面板换技出战。技能执行不逐角色硬编码——引擎新增 `_execLearned()` 通用执行器，按技能数据字段（`dmg/push/stun/seal/apCut/heal/poison/silence/apGain/empower`）与技能种类（`unit/adj/raoe/self`）数据驱动执行。
- **被动继承（获得角色 = 获得其全部技能）**：装备谁的刀卡，其被动机制即对音克思生效。实现为引擎级 `passiveOwner(u)` 单一入口——装备时 `Blades.applyBoons` 在玩家单位上标 `_learnedFrom`，引擎 25 处 `charId` 被动判定（鲁豪刀击翻倍/头哥免疫击退/wonder 血祭两连/仙女免疫晕眩/李帆免动购刀/歆慧偶数回合+1/大师回合回血/各免死与斩杀类）统一改走该口；主动技仍走 `_execLearned`。基础血量不随继承（按音克思自身成长）。专项测试 `test_passives.mjs` 11 断言（含未装备对照与敌方不受影响对照）。
- **修炼（买道具提升自己）**：七项永久强化（体魄×3/刀锋/马政/轻功/吐纳/算学/气力），零花钱购买、价格随级递增，全部映射到引擎既有增益（`b_knife/b_dodge/...`）注入，零新引擎概念；首购授成就「有备而来」。
- **稀有刀卡**：6 张质变被动（以道代血/刀扫一片/长杆马刀/庆功之宴/班主任的偏爱/先手刀），高难试炼首通所授、每场常驻——复用马刀风云生存模式的稀有增益池，把"通关奖励"变成"角色解锁"。
- **段位**：12 档（未入册 → 执刀生 → 九品…… → 马刀之神），胜场驱动。
- **战大道具**：行囊中护手霜/黑棒/玉米/水壶/陀螺映射到既有增益，约战对话中择一携带，开战时注入，用后销毁。

### 2.1b 高难试炼——"难在机制，不在数值"的特则关卡

六座关卡（`main.js TRIALS`），按"章节 + 已胜传主 + 前置剧情"渐次解锁，关卡配置复用马刀风云原关卡地形；每一座的核心难度来自一条**剧情特则**（引擎级新机制，非数值放大）：

| 试炼 | 特则 | 机制与解法 |
|---|---|---|
| 运动会·不怒自威 | `cans` 看台飞瓶 | 回合开始与敌人同行/同列者被砸 1 血——走位解法 |
| 宿舍之夜·双臭临门 | `stench` 鲍鱼之肆 | 回合结束相邻敌我互相腐蚀各损 1 血——贴身即换血，逼远程 |
| 九省联考·牛刀小试 | `yansuan` 验算 | 回合末 wonder 血量为偶数则回 1 血——伤害奇偶规划 |
| 禁闭室·疯法同囚 | `suomen` 锁门 | 无墙可踢，双方马踢不可用——禁用核心机制，纯刀技缠斗 |
| 二楼巡征·羚羊 | `zhengshi` 争食 | 场上三份饭，回合末站上者回 2 血、每份一次——地图资源争夺 |
| 终焉·本纪重演 | `zhongshu` 种树不绝 | 崇国每回合自动种树（≤3 株）——持续场面压力与回血口粮 |

特则全部实现在战斗引擎（`run` 回合开始 / `_endRound` 回合结束 / `doHorse` 动作校验），沿用既有 `cfg.rule` 通道，**DOM 无关、可 Node 直测**；战场开场战报自动播报特则，试炼面板常驻展示。专项测试 `test_rules2.mjs` 八项断言（含"无特则不误伤"回归）全 PASS。首通授稀有刀卡并计入成就「试炼者」；再打无赏，纯为切磋。

### 2.2 战斗覆盖层（battle-ui.js）——自 1497 行单体 UI 解耦

马刀风云的战棋渲染/HUD/输入与菜单系统耦合在 `ui.js`。解耦原则：**保留全部引擎钩子实现，删除全部菜单**。

- 保留：Canvas 渲染管线（宣纸底/砖墙/角楼旗帜/障碍地形/粒子/斩击/弹道/幽灵/暗角）、HUD 刷新、玩家阶段状态机、猜拳弹层、增益三选一、战斗内对话（打字机）、触屏适配。
- 替换：屏幕管理改为**全屏覆盖层**（`#battle`，世界循环以 `window.BATTLE_ACTIVE` 暂停）；结算页不再直连关卡列表，改为渲染引擎统计 + `SJI_BATTLE_HOOKS.onResult` 返回的奖励/史评文案；说话人解析器统一马刀剧本键（`豪/铭/上/问…`）与世界人物（`PEOPLE_BY_ID/MINOR_FIGS`）。
- 自启动：`DOMContentLoaded` 自绑事件（马刀原版由其 main.js 启动，本作无此文件）。

### 2.3 任务驱动的主线（quests.js）——9 节高难关 + 7 条条件支线

用户要求"完全重构剧情：开局即主线，给任务指引，满足条件进支线拿强力人物，主线都是高难关卡"。落地为 `js/quests.js` 的三张数据表 + 一套指引/结算逻辑：

- **难度自选（DIFFS）**：四档（0.8/1.0/1.3/1.6）。难度写入战斗层设置 `SJI_SAVE.settings.lastDiff`，战斗引擎直接读（敌方行动点/伤害/血量/击破回血/狂攻），**任务赏格同步按倍率放大**——难度不是宽容度开关，而是收益与风险的同一个旋钮。开卷前弹出选择面板，之后可在设置里随时改。
- **主线 9 节（MAIN）**：初执马刀 → 实验三异能者 → 操场三国刀 → 世界马刀协会 → 七班刀合流 → 刀禁令风波 → 马刀之神 → 终焉之战 → 马刀的结局。每节皆高难关卡，形态是"**复用特则机制做 Boss 战**"：连波（`waves` 三阵连战、阵间回血）、血量缩放（`hpScale` 0.62–1.6）、强制难度（`diff: 'hard'`）、以及在 §2.1b 那六条剧情特则上继续编排（`dyad` 二声部合唱 / `uprising` 起义援军 / `chaos` 同门相争）。
- **支线 7 条（SIDE）**：触发条件是**好感 / 胜场 / 战绩记录**三类（`cond()` 纯函数判定：`Engine.favorOf('dage') >= 20`、`(G.wins||0) >= 10`、`Blades.hasCard('luhao')`）。完成支线解锁**强力人物**（进 `G.roster` 出战名册）或稀有刀卡。
- **点将出征（`pickAndStart`）**：名册多于一人时，接战前弹出选择面板。音克思保有刀谱与修炼加成；他人以本卡出战，但**稀有刀卡与道具按名册共享**——把"解锁人物"做成真的可用，而不只是图鉴条目。
- **指引 UI**：HUD 右侧 `#questcard` 常驻显示当前主线（名称/目标/提示）+ 可接支线 + 「前往」一键直达（跨场景传送 + 自动寻路走位）；世界地图上画出「令」（主线，金）/「刀」（支线，绿）气泡，可点击、可靠近出交互条。
- **赏格结算（`complete`）**：零花钱/声望按难度倍率结算、稀有刀卡入谱、强力人物入册、成就「听令行事」、自动推进主线并刷新指引卡。

### 2.3b 时间线解耦——把"睡觉推进章节"换成"主线推进章节"

原架构的骨架是「17 章 × 每天 4 时段 × 行动点」：`G.ch` 靠睡够天数递增，事件按时段窗口出现，探索要花行动点。这与"自由开放世界"直接冲突。重构做法是**把时间线降级为风味，把任务升级为骨架**：

| 维度 | 原 | 现 |
|---|---|---|
| 章节 `G.ch` | 睡够 `chDef().days` 天 → 下章 | `Quests.chapterNow()` 由主线完成度映射（m1→1、m4→8、m6→12、m9→16），`Engine.syncChapter()` 单向推进，只进不退 |
| 事件可见性 | `e.ch + e.day + e.periods` 三重门控，错过即永久错过 | 只按章过滤（`e.ch === G.ch && !done`），**本章之事皆可亲历**；已走过的章节漏掉且未收集的史料仍可打听 |
| 行动点 | 读书/听课/采访/打听皆耗 AP，不足则禁行 | `spendAP()` 恒真（保留接口不动调用点），HUD 原 ⚡ 位改显示**主线进度 n/9** |
| 时段 | 门控风险与事件 | 纯氛围：只影响画面色调与 NPC 流动，每天随机轮换 |
| 巡查风险 | 仅上午/下午课、且教室外 | 只与地点有关（教学楼内 9%），不再限时段 |
| 推进按钮 | 「下一时段 ▸」（4 次一循环） | 「歇一日 ▸」随时可用：写日记/夜谈/早睡三选一后 `nextDay()` |
| 立传 | 主线门禁（未胜传主不可立传） | 可选支线，`Writing.duelReady()` 恒真，`duelWon()` 降级为面板软提示 |

关键收益是**映射表代替了时序耦合**：协会开张、刀禁期、试炼解锁、番外人物客串这些既有内容全部只认 `G.ch` 阈值，于是"打 m4 → 协会开张""打 m6 → 刀禁期起"自动成立，无需逐个改判断；老档也天然兼容（`syncChapter` 只进不退，`Engine.syncWorldFlags()` 按当前 `G.ch` 补齐解锁）。

- **成传战解耦**：原「未胜传主不可立传」的门禁撤下，`Writing.duelReady()` 恒真、`Writing.duelWon()` 降级为面板软提示（"未与传主一战——不影响立传，胜之另可录其技"）。立传从"主线必经"变成"随时可写的支线玩法"。
- **首战即首戏**：27 个角色映射到马刀风云剧本卷（`SCENE_OF_CHAR`），首次约战自动挂接该卷战前台词，胜利/失败自动接战后评——两作台词资产全量复用。
- **终焉之战**：主线 m8 与挑战校长均触发 `s14` 特殊战场（钦法+校长双人、为兵助阵、校长室柜子地形、hpScale 0.78）。
- **刀禁期**：`G.ch ≥ 12`（即 m6 刀禁令风波之后）起"师怒禁刀"。楼内约战 25% 触发钦法巡查，三选一应对，失败"缴刀两日"（`flags.duelBanDays`，约战全 blocked，次日递减）——把第一个游戏的世界观结局（禁刀）变成第二个游戏的风险系统。
- **终章**：主线九节全成后，任务卡转为「刀史收卷」并出现「终章 · 高考」入口。AI 读史结局沿用春秋笔，新增刀的三档结局变体（0 胜/普通/30+胜），收在"写进书里的刀，不会消"。

### 2.4 其他模式与内容

- **名场面事件**：68 条校园名场面（场景绑定、亲历得料、长好感）改为"本章皆可见"，不再受时段窗口限制；漏过的史料可托人打听（好感≥15，转述版文笔-1）。这是世界层的风味与史料来源，与任务系统并行不悖。
- **锦标赛**：`waves` 三连战（鲁豪→小川→子琛，`restFull` 充分休整，hpScale 0.68），胜利三技全录+冠军成就。协会随主线 m4 开张（`Engine.syncWorldFlags()` 按 `G.ch ≥ 8` 置 `flags.xiehui`），不再依赖某条事件被触发。
- **生存**：引擎 survival 模式直通（波次+三选一增益），按波次给零花钱，3 波起计胜场。
- **高难试炼**：六座关卡，解锁条件统一为「`G.ch` 阈值 + 已胜某传主」，因此随主线自然渐次开放。

## 3. 测试体系

| 层 | 内容 | 结果 |
|---|---|---|
| 引擎层（Node，无需浏览器） | 前作 15 个测试整体移植：规则/状态/极难/AI 四档/平衡基线/模糊测试/压测；本章新增特则专项 `test_rules2.mjs`（8 断言）与被动继承专项 `test_passives.mjs`（11 断言，含未装备/敌方不受影响两组对照） | 全部 ALL PASS（路径修复 `tests/battle/helpers/engine_env.mjs`） |
| 主线平衡采样 | `mainquest_winrate.mjs`：9 节 × 4 难度 × N 场的 greedyBot 胜率表，**直读 `window.Quests` 真配置**（脚本不再维护副本）；玩家按"到该节时的大致成长"分档建模 | 调平后无 easy/normal 0% 项；调参记录见 §4.1 |
| 剧情链路（Playwright） | `tests/quest_flow.mjs` 30 项：择难度→开局即主线→赏格随难度→章节映射链（m4 协会/m6 刀禁/m9 终章）→歇一日→立传解耦→支线触发与解锁→点将出征→任务点错位 | 全 PASS |
| 全链路（Playwright） | `smoke.mjs` 56 项断言：装配→**择难度开局**→**任务卡/令标记/进度**→约战→战斗→结算→回世界→刀谱→**立传解耦**→首战剧本→**主线任务端到端（接战→胜→赏格×难度→推进 m2→卡片刷新）**→锦标赛三波→生存波次→弹层治理→白板/修炼购买→试炼解锁与首通稀有卡；对**源码版**与**打包单文件版**双跑 | 全 PASS，截图 17 张入 `testshots/` |

引擎层的可测性红利在本项目直接兑现：融合层对引擎的改动（`yinkesi` 通用技能、`_nextWave` 波次号时序、六条剧情特则、测试路径）全部被既有测试拦截回归，无需人工比对。

> 注：`tests/quest_flow.mjs` 需要 Playwright；本机安装方式见 §5。

## 4. 开发过程中的真实缺陷档案

| # | 缺陷 | 根因 | 修复 | 回归 |
|---|---|---|---|---|
| 1 | 约战后战场永不打开（`BattleUI is not defined`） | main.js 引用了不存在的模块别名 | 统一走 `SJI_UI.startBattle` | smoke |
| 2 | 战斗按钮/画布/键盘全部无响应 | `SJI_UI.boot()` 无人调用（马刀原版由其 main.js 启动） | battle-ui 自启动 | smoke |
| 3 | 结算按钮"可见但点不到" | `#battle` 一直带着初始 `hidden` 类隐形渲染，`showOverlay` 只切 `on` | show/hide 同步切 `hidden` | smoke + 截图 |
| 4 | 结算页出现但按钮不可见 | 显示结算时战场视图未隐藏，内容溢出滚动区 | `showScreenResult` 同步隐藏 `#battle-stage` | smoke |
| 5 | 老存档/新字段冲突 | `const` 全局不挂 `window`，跨模块以 `window.Blades` 互访恒为 undefined | 改用 `typeof Blades` 判活；刀谱/门禁/图鉴三处 | smoke（门禁用例） |
| 6 | 生存模式"卡在增益弹层" | 波次号在增益选择**前**递增，观察者误判已入下一波 | `_nextWave` 将 `survivalWaveNo` 推移移到 `pickBoon` 之后 | stress + smoke |
| 7 | 场景动作（读书/购物/协会入口）在无 NPC 处不出现 | `updateCtx` 以 `if (!near) return` 早退 | 场景动作独立于 near 判活 | smoke |
| 8 | 测试中约战时灵时不灵 | 80 距离外约战语义是"先走近" | 测试改为轮询 challenge 直至对话弹出 | smoke |
| 9 | 任务完成后指引卡不刷新，页面报 `render is not defined` | `Quests.complete()` 裸调全局 `render()`，应为 `this.render()` | 改 `this.render()` | quest_flow + smoke |
| 10 | **HUD 段位章永远显示「未入册」** | `(window.Blades && Blades.rankName()) \|\| '未入册'`——`window.Blades` 恒 undefined，短路后永远走兜底值。旧断言只查 `textContent.length > 0`，被"未入册"蒙混过关 | 改 `typeof Blades !== 'undefined' && Blades.rankName()`；断言改为**不得含「未入册」** | smoke（新断言当场抓出） |
| 11 | **设置面板里的难度选择器从不渲染** | 同上：`if (window.DIFFS && window.SJI_SAVE)` 恒假，整段难度设置被静默跳过 | 改 `typeof DIFFS !== 'undefined'` | quest_flow（断言四档齐列） |
| 12 | 战斗内对话的说话人头像缺失 | `window.PEOPLE_BY_ID` 恒 undefined，真人一律退化到 `MINOR_FIGS` 兜底 | 改 `typeof PEOPLE_BY_ID !== 'undefined'` | smoke |
| 13 | 战大道具用后不落盘 | `if (window.Save) Save.write()` 恒假，道具销毁状态未写回存档，重载后道具"复活" | 改 `typeof Save !== 'undefined'` | 代码审查 |
| 14 | 静音开关无效 | `Sfx.tone` 查 `G.muted`，实际字段是 `G.settings.muted` | 改读 `G.settings.muted` | 代码审查 |
| 15 | 同场景两个任务点叠在一起，只能点到其中一个 | `Quests.markers()` 直接返回各任务原始坐标，`nearMarker` 与点击命中都取"最近一个"，重叠点互相遮蔽 | 新增显示位置 `dpos`：同场景距离 <70 的点按扇形摊开（左右各约 42px），绘制/点击/靠近检测统一用 `dpos`，寻路仍用原锚点 `q.pos` | quest_flow（两点分别命中） |

10–14 是同一类根因的连锁爆发（`const` 全局不挂 `window`），共 5 处；它们的共同特征是**不报错、只静默走兜底分支**，因此旧测试全部通过。教训写进 README「设计约定」，并新增了针对性的正向断言。

### 4.1 主线平衡采样记录（kiteBot，12 场/格）

`mainquest_winrate.mjs` 直读 `window.Quests` 真配置采样。调平动作与结果：

| 节 | 调整 | 终态胜率 E/N/H/X |
|---|---|---|
| m2 三异能者 | 加 restFull，hpScale 1→0.75 | 92/67/17/0 |
| m3 三国刀 | 双人同场→**先后单挑+阵间回血**（greedy/kite 都不会拆双人阵），hpScale→0.65；特则 dyad→chaos（三国相争，贴合主题且利玩家） | 100/17/8/0 |
| m4 锦标赛 | hpScale 0.68→0.45 | 100/67/17/8 |
| m5 七班刀 | 加 hpScale 0.85（极难 0%→8%） | 100/58/58/8 |
| m6 刀禁令 | 加 hpScale 0.9 | 100/92/83/42 |

- m3 normal 17% 为**有意保留的风筝教学关**：鲁豪刀击翻倍惩罚贴身换血，kiteBot 拉扯后 8%→17%，人类玩家可正常拆解；继续压数值会毁掉这一节的教学意义。
- m2/m3 极难 0%：极难档（5 动/狂攻/无击破回血）叠加白板期本就近似无解，属设计上的"别在新档选极难"，已在玩法说明注明。
- 教训入档：**greedyBot 对"高被动倍率近战"（鲁豪）集体 0 分，不能作为唯一平衡依据**——bot 换成 kiteBot 后 m3 easy 从 13%→100%，同配置同种子。

## 5. 已知取舍与技术债

- 世界循环在战斗期间仅逻辑暂停，渲染仍以覆盖层遮挡（省一次重构，开销可忽略）。
- 战斗侧偏好（速度/AI/猜拳模式/特效）暂存 `shiji_madao_v1`，未并入世界设置面板——两套 localStorage 并存，导出互不影响。
- 战斗断点续战（马刀原版功能）在开放世界流中未接入口，快照机制保留。
- 引擎中约 40 处 `charId ===` 硬编码被动（继承自马刀风云已知技术债），`yinkesi` 未新增同类分支。
- **时段字段未删干净**：`G.periodIdx` 仍存在且被 NPC 调度与画面氛围读取（每天随机轮换），仅门控语义被移除。若将来要彻底去时段化，需一并改 `World.placement()`。
- **`G.ap` / `chDef().days` 退化为死字段**：`spendAP()` 恒真、`nextDay()` 不再查天数上限，两个字段保留只为老档兼容，未从存档结构中摘除。
- **测试环境依赖本机 Playwright**：仓库不含 `node_modules`，`smoke.mjs` / `tests/quest_flow.mjs` 需 `NODE_PATH` 指向装有 `playwright` 的目录（本机为 `C:\Users\LENOVO\.workbuddy\binaries\node\workspace\node_modules`）。建议后续加 `package.json` 固化依赖。
- **技术报告 PDF 的重生成依赖本机 Word + pywin32**：`export_pdf.py` 走 `win32com` 调用 Word；本机缺少 `pywin32` 时只能重生成 `.docx`。

## 6. 交付物

| 文件 | 说明 |
|---|---|
| `实验史记·马刀行.html` | 单文件成品（build.py 产物，双击即玩） |
| `实验史记·马刀行-玩法说明.txt` | 玩家向说明书 |
| `README.md` / `技术报告.md(.docx/.pdf)` | 开发者文档 |
| `js/ css/ tests/ build.py smoke.mjs tests/quest_flow.mjs` | 全部源码与测试 |
| `tests/battle/engine/*.mjs` | 15 个引擎 Node 测试 |
| `testshots/*.png` | 冒烟验收截图 |
